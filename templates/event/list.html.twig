{% extends 'base.html.twig' %}

{% block title %}Fortil | Back Office - Event{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/backoffice.css') }}">
{% endblock %}

{% block body %}
<div class="main-container backoffice">
    <div class="topbar">
        <div class="svg-container">
            <a class="stretched-link" href="{{ path('app_backoffice') }}">
                <img class="svg" src="{{ asset('svg/arrow-left-solid.svg') }}" alt="Left arrow SVG" width="40">
            </a>
        </div>
    </div>

    <section class="container">
        <h1 class="main-title">Events</h1>

        <table class="table">
            <thead>
                <tr>
                    <th class="table-cell">Name</th>
                    <th class="table-cell">Start</th>
                    <th class="table-cell">End</th>
                    <th class="table-cell">Actions</th>
                </tr>
            </thead>

            <tbody>
                {% for event in events %}
                    <tr>
                        <td class="table-cell">{{ event.name }}</td>
                        <td class="table-cell">{{ event.startDate|date("d/m/Y H:i") }}</td>
                        <td class="table-cell">{{ event.endDate|date("d/m/Y H:i") }}</td>
                        <td class="table-cell">
                            <a class="btn btn--gradient btn--dashboard" href="#">Edit</a>
                            <a class="btn btn--gradient btn--dashboard" href="#">Delete</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </section>
</div>
{% endblock %}
